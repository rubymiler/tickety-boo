<%= form_for [ticket, task] do |f| %>
  <%= f.text_field :description, class: 'input-medium' %>
  <%= f.collection_select :assignee_id, User.agent, :id, :name, prompt: true, class: 'select-long' %>
  <%= f.submit '+', class: 'btn btn-pink' %>
<% end %>